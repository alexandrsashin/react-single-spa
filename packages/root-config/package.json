{
  "name": "root-config",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite --port 3005",
    "build": "vite build && node scripts/post-build.js",
    "build:nocheck": "vite build && node scripts/post-build.js",
    "lint": "eslint src --ext js,ts,tsx",
    "format": "prettier --write .",
    "check-format": "prettier --check .",
    "build:types": "tsc",
    "preview": "vite preview"
  },
  "dependencies": {
    "single-spa": "^5.9.3",
    "single-spa-layout": "^1.6.0",
    "vite": "^4.4.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "types": "dist/example-root-config.d.ts"
}
